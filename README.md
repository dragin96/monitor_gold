# 🛍️ GoldApple Category Tracker Bot

Телеграм-бот для отслеживания изменений количества товаров в категориях на [goldapple.ru](https://goldapple.ru)

## 📋 Возможности

- 🔍 Отслеживание количества товаров в категориях по categoryId
- 📬 Автоматические уведомления при изменении количества товаров
- 📊 Управление списком отслеживаемых категорий
- ⏰ Периодическая проверка каждые 5 минут (настраивается)
- 💾 Сохранение подписок между перезапусками
- 📈 Уведомления об увеличении или уменьшении количества товаров

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd gold_apple
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Создайте Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота

### 4. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните `.env`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
CHECK_INTERVAL=5
```

### 5. Запустите бота

```bash
npm start
```

Для разработки с автоперезагрузкой:

```bash
npm run dev
```

## 📖 Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/track <categoryId>` - Отслеживать категорию по ID
- `/list` - Показать все отслеживаемые категории
- `/remove <categoryId>` - Удалить категорию из отслеживания
- `/check <categoryId>` - Проверить количество товаров в категории прямо сейчас

### Как найти categoryId

1. Откройте нужную категорию или бренд на goldapple.ru (например, `/brands/flacon-magazine`)
2. Откройте DevTools (F12) → вкладка **Network**
3. Обновите страницу
4. Найдите запрос к `/front/api/catalog/cards-list`
5. В теле запроса (Payload) найдите `"categoryId": 1000001798`

**Пример для бренда Flacon Magazine:** categoryId = `1000001798`

### Примеры использования

```
/track 1000001798
/list
/check 1000001798
/remove 1000001798
```

## 🔧 Настройка

### Изменение интервала проверки

Измените значение `CHECK_INTERVAL` в файле `.env` (в минутах):

```env
CHECK_INTERVAL=10  # Проверка каждые 10 минут
```

## 📁 Структура проекта

```
gold_apple/
├── src/
│   ├── index.js         # Точка входа
│   ├── bot.js           # Логика Telegram бота
│   ├── goldappleApi.js  # API клиент GoldApple
│   ├── tracker.js       # Система отслеживания
│   └── database.js      # Работа с базой данных
├── data/
│   └── subscriptions.json  # База подписок
├── package.json
├── .env.example
├── .gitignore
└── README.md
```

## 🔐 Безопасность

- Никогда не коммитьте файл `.env` с токенами
- Храните токен бота в секрете
- Используйте `.gitignore` для исключения конфиденциальных данных

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте, что токен правильный в `.env`
2. Убедитесь, что бот запущен
3. Проверьте логи на наличие ошибок

### Уведомления не приходят

1. Убедитесь, что количество товаров в категории действительно изменилось
2. Проверьте, что бот запущен и работает
3. Проверьте значение `CHECK_INTERVAL` - возможно нужно уменьшить интервал
4. Проверьте логи - там отображаются все изменения

### Ошибка "Cannot find module"

Убедитесь, что установлены все зависимости:

```bash
npm install
```

### Как проверить categoryId правильный

Используйте команду `/check <categoryId>` - бот покажет текущее количество товаров

## 🛠️ Технологии

- Node.js
- [node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api) - Telegram Bot API
- [node-cron](https://github.com/node-cron/node-cron) - Планировщик задач
- [dotenv](https://github.com/motdotla/dotenv) - Управление переменными окружения

## 📝 TODO

- [ ] Добавить веб-интерфейс для управления
- [ ] Поддержка множественных категорий одновременно
- [ ] Графики изменения количества товаров
- [ ] Экспорт истории изменений
- [ ] Уведомления о конкретных товарах в категории

## 📄 Лицензия

MIT
