# 🛍️ GoldApple Category Tracker Bot

Телеграм-бот для отслеживания изменений количества товаров в категориях на [goldapple.ru](https://goldapple.ru) с использованием браузерной автоматизации.

## 📋 Возможности

- 🌐 **Браузерная автоматизация** - использует Puppeteer для обхода защиты от ботов
- 🔍 **Отслеживание категорий** - мониторинг количества товаров в реальном времени
- 📬 **Автоматические уведомления** - подписка на изменения в категориях
- 📊 **История изменений** - сохранение до 100 последних изменений по каждой категории
- ⏰ **Cron-расписание** - автоматические проверки каждый час (настраивается)
- 💾 **JSON хранилище** - простое хранение данных без базы данных
- 🎭 **Stealth режим** - множество техник обхода детекции автоматизации
- 📈 **Статистика** - процент изменения и разница в количестве товаров

## 🚀 Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd gold_apple
```

### 2. Установите зависимости

```bash
npm install
```

Основные зависимости:
- `node-telegram-bot-api` - для работы с Telegram Bot API
- `puppeteer` - для браузерной автоматизации
- `node-cron` - для планирования задач

### 3. Создайте Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота

### 4. Настройте переменные окружения

```bash
export TELEGRAM_BOT_TOKEN="your_bot_token_here"
```

### 5. Запустите бота

**Вариант 1: Полный бот с автоматическими проверками**

```bash
node example-bot.js
```

**Вариант 2: Простая проверка категорий (без бота)**

```bash
node example-simple.js
```

**Вариант 3: Тестовый запуск**

```bash
node src/bot-test.js
```

## 📖 Использование

### Команды бота

- `/start` - Приветственное сообщение и список команд
- `/help` - Справка по всем командам
- `/check <category>` - Проверить конкретную категорию (например: `/check flacon-magazine`)
- `/checkall` - Проверить все категории сразу
- `/categories` - Показать список всех доступных категорий
- `/subscribe` - Подписаться на автоматические уведомления об изменениях
- `/unsubscribe` - Отписаться от уведомлений
- `/status` - Показать статус трекера и количество подписчиков

### Примеры использования

```
# Проверить категорию Flacon Magazine
/check flacon-magazine

# Проверить все категории
/checkall

# Посмотреть доступные категории
/categories

# Подписаться на уведомления
/subscribe
```

### Добавление новых категорий

```javascript
import { addCategory } from './src/categoryTracker.js';

addCategory(
  'new-brand',                           // Ключ категории
  'New Brand Name',                      // Название
  'https://goldapple.ru/brands/new-brand' // URL
);
```

## 🔧 Настройка

### Изменение интервала проверки

Отредактируйте файл [src/tracker-browser.js](src/tracker-browser.js):

```javascript
// Проверка каждые 2 часа вместо каждого часа
this.cronJob = cron.schedule('0 */2 * * *', async () => {
  await this.checkAllCategoriesAndNotify();
});
```

### Настройка браузера

Отредактируйте файл [src/goldappleApiBrowser.js](src/goldappleApiBrowser.js):

```javascript
// Изменить на headless: true для работы в фоновом режиме
browser = await puppeteer.launch({
  headless: true,  // Поменяйте на true
  // ...
});
```

## 📁 Структура проекта

```
gold_apple/
├── src/
│   ├── categoryTracker.js       # Управление категориями и сравнение
│   ├── storage.js               # JSON хранилище данных
│   ├── goldappleApiBrowser.js   # Браузерная автоматизация (Puppeteer)
│   ├── bot-test.js              # Тестовая реализация команд
│   ├── bot-browser.js           # Telegram бот
│   └── tracker-browser.js       # Трекер с cron-расписанием
├── data/
│   ├── category-counts.json     # Текущие данные категорий
│   └── *-history.json           # История изменений по категориям
├── example-bot.js               # Пример: полный бот
├── example-simple.js            # Пример: простая проверка
├── test-browser.js              # Тест браузера
├── BOT_IMPLEMENTATION.md        # Подробная документация модулей
├── ANTIBOT_SOLUTIONS.md         # Решения проблемы блокировки
└── README.md
```

## 📚 Документация

- **[BOT_IMPLEMENTATION.md](BOT_IMPLEMENTATION.md)** - подробная документация всех модулей и примеры использования
- **[ANTIBOT_SOLUTIONS.md](ANTIBOT_SOLUTIONS.md)** - решения проблемы блокировки сайтом

## 🔐 Безопасность

- Никогда не коммитьте файл `.env` с токенами
- Храните токен бота в секрете
- Используйте `.gitignore` для исключения конфиденциальных данных

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте, что токен правильный в `.env`
2. Убедитесь, что бот запущен
3. Проверьте логи на наличие ошибок

### Уведомления не приходят

1. Убедитесь, что количество товаров в категории действительно изменилось
2. Проверьте, что бот запущен и работает
3. Проверьте значение `CHECK_INTERVAL` - возможно нужно уменьшить интервал
4. Проверьте логи - там отображаются все изменения

### Ошибка "Cannot find module"

Убедитесь, что установлены все зависимости:

```bash
npm install
```

### Как проверить categoryId правильный

Используйте команду `/check <categoryId>` - бот покажет текущее количество товаров

## 🛠️ Технологии

- **Node.js** - основная платформа
- **[Puppeteer](https://pptr.dev/)** - браузерная автоматизация для обхода защиты
- **[node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)** - Telegram Bot API
- **[node-cron](https://github.com/node-cron/node-cron)** - планировщик автоматических проверок
- **JSON** - простое файловое хранилище без базы данных

## 🎯 Архитектура

Проект разделен на **переиспользуемые модули**:

1. **categoryTracker.js** - логика работы с категориями
2. **storage.js** - сохранение и загрузка данных
3. **goldappleApiBrowser.js** - браузерная автоматизация
4. **bot-test.js** - готовые обработчики команд
5. **bot-browser.js** - Telegram бот
6. **tracker-browser.js** - автоматические проверки по расписанию

Каждый модуль можно использовать отдельно или комбинировать.

## ⚡ Быстрый старт

```javascript
import { BrowserGoldAppleBot } from './src/bot-browser.js';
import { BrowserCategoryTracker } from './src/tracker-browser.js';

const bot = new BrowserGoldAppleBot(process.env.TELEGRAM_BOT_TOKEN);
const tracker = new BrowserCategoryTracker(bot);

bot.setTracker(tracker);
tracker.start();

console.log('Бот запущен!');
```

## 📝 Возможные улучшения

- [ ] Интеграция с `puppeteer-extra-plugin-stealth` для лучшего обхода защиты
- [ ] Поддержка прокси для смены IP
- [ ] Веб-интерфейс для управления
- [ ] Графики изменения количества товаров
- [ ] Экспорт истории в CSV/Excel
- [ ] Уведомления о конкретных товарах
- [ ] База данных вместо JSON (PostgreSQL/MongoDB)

## 📄 Лицензия

MIT
